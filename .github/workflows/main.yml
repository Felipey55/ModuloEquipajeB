name: Java CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: List root directory
        run: ls -R /home/runner/work/ModuloEquipajeB/ModuloEquipajeB/

      - name: List files in ModuloEquipaje directory
        run: ls -R ./ModuloEquipajeB

      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '11'

      - name: Cache Maven packages
        uses: actions/cache@v2
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Create GraphQL client directory
        run: mkdir -p ./ModuloEquipajeB/src/main/resources/graphql-client

      - name: Install dependencies
        run: mvn install -DskipTests
        working-directory: ./ModuloEquipajeB

      - name: Run tests
        run: mvn test
        working-directory: ./ModuloEquipajeB

      - name: Run Sonar analysis
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=Felipey55-ModuloEquipajeB -Dsonar.jacoco.reportPaths=target/jacoco.exec || true
        working-directory: ./ModuloEquipajeB
